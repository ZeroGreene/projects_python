#programmer: Zero Greene
#company: Greene Aeration

#~~~Beginning of code~~~

#~~~~~beginning of def is_prime(n):~~~~~
def is_prime(n):
    if n <= 1: #2 is the smallest prime integer
        return False #false = not a prime

    for i in range(2, int(n ** 0.5) + 1):
        if n % i == 0: #no remainder
            return False 

    return True
#~~~~~end of def is_prime(n):~~~~~

# Main program loop
while True:
    user_input = input("Please type a number to check if it's prime, enter to compute. Type 'exit' to quit: ")
    
    if user_input.lower() == 'exit':
        print("Thanks for stopping by")
        break

    if not user_input.isdigit():
        print("Please enter a valid positive integer.")
        continue

    num = int(user_input)

    if is_prime(num):
        print(f"{num} is a prime number.")
    else:
        print(f"{num} is not a prime number.")

#~~~End of code~~~
